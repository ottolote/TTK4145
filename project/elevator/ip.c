#include "ip.hpp"
#include <iostream>

enc_ip encode_ip(std::string ip_string) {

    std::cout << ip_string.find('.') << std::endl;
    
    ip_string.rfind( ip_string);

    return 0;
}


std::string decode_ip(enc_ip encoded_ip) {
//    std::string ip_string;
//    unsigned char bytes[4];
//    bytes[0] = ip & 0xFF;
//    bytes[1] = (ip >> 8) & 0xFF;
//    bytes[2] = (ip >> 16) & 0xFF;
//    bytes[3] = (ip >> 24) & 0xFF;	
//    ("%d.%d.%d.%d\n", bytes[3], bytes[2], bytes[1], bytes[0]);
//    
//
//    return ip_string;
}
